spring.application.name=roratusAnexo

spring.profiles.active=dev

server.port=8081

spring.datasource.username=${ORACLE_DB_LOGIN}
spring.datasource.password=${ORACLE_DB_PASSWORD}
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.OracleDialect

spring.jpa.open-in-view=false

server.error.include-message=always
spring.jpa.hibernate.ddl-auto=none

spring.datasource.dbcp2.initial-size=7
spring.datasource.dbcp2.max-total=20
spring.datasource.dbcp2.pool-prepared-statements=true

ftp.server.dir=erp-repo.ajrorato.ind.br
ftp.server.port=21
ftp.server.user=erp
ftp.server.password=enHu]Yw}Mb>_(4])U]N2

spring.servlet.multipart.enabled=true

#Defina o tamanho máximo de um arquivo que pode ser armazenado na memória
spring.servlet.multipart.file-size-threshold=1MB

#Tamanho máximo permitido para cada arquivo individual enviado.
spring.servlet.multipart.max-file-size=10MB

#Tamanho máximo da requisição multipart como um todoo
spring.servlet.multipart.max-request-size=10MB

#Define o tamanho máximo de dados que o Tomcat irá aceitar em uma requisição POST, mesmo que a requisição seja rejeitada.
server.tomcat.max-swallow-size=10MB

server.tomcat.threads.max=200
